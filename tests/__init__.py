"""Test package for the athletics scoring system.

Making ``tests`` a package lets ``unittest discover`` run with an explicit
top-level directory (``-t .``) so imports of ``athletics_scoring`` and
``webapp`` resolve deterministically in CI across platforms.
"""
